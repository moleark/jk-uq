OPEN TUID Member(
    id _,
    recommendationCode char(10),
);

OPEN MAP MemberRecommender(
    key member ID Member,
    key referrer ID Member,
);

BOOK Point(
    key member ID Member,
    point bigint,
);

HISTORY PointHistory(
    date,
    key member ID Member,
    source char(100),
    point bigint not null,
);