QUERY SearchPaperDoc (domain ID DomainType, documenttype ID DocumentType )
returns ret (
    id bigint,
    name char(100),
    code char(10),
    image char(100),
    description char(50)
) {
    into ret SELECT id,name,code,image
    FROM    Document
    WHERE   domain=domain and documenttype=documenttype
    order BY id; 
};